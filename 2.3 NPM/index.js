// NODE PACKAGE MANAGER(library) tools created from NATIVE NODE MODULE BY PEOPLE

// var generateName = require('sillyname');

import * as sup from "superheroes";
import {randomSuperhero} from "superheroes";
var name = sup.randomSuperhero();

console.log(`I am ${name}`);